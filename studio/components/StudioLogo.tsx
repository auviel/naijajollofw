import logo from '../static/logo.png?url'

/** Square Studio navbar icon (Sanity `icon` prop). */
export function StudioLogo() {
  return <img src={logo} alt="Naija Jollof" width={25} height={25} />
}
