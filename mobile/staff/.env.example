# Next.js origin. On a physical device use your computer's LAN IP, not localhost.
EXPO_PUBLIC_API_URL=http://localhost:3000

# Sentry (project: naijajollofw-kitchen). Leave empty to disable.
EXPO_PUBLIC_SENTRY_DSN=
# Optional: upload source maps / dSYMs during EAS builds (never commit the token).
# Without this, set SENTRY_DISABLE_AUTO_UPLOAD=true in eas.json (already set for preview/production).
# SENTRY_AUTH_TOKEN=
