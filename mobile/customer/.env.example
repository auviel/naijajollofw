EXPO_PUBLIC_API_URL=http://192.168.2.63:3000
# Square application id comes from GET /api/checkout/config (NEXT_PUBLIC_SQUARE_APPLICATION_ID on the server).
# In-app card entry needs an EAS dev/preview/store build — Expo Go falls back to website checkout.
