# Manual only — run with:
#   cd mobile/customer && npx eas-cli workflow:run .eas/workflows/preview.yml
# Not triggered on git push (saves Expo free-plan build minutes).

name: Naija Jollof preview

jobs:
  build_ios:
    name: Build Naija Jollof iOS
    type: build
    params:
      platform: ios
      profile: preview
  build_android:
    name: Build Naija Jollof Android
    type: build
    params:
      platform: android
      profile: preview
